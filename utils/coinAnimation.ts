/** Shared timing so the coin-flight in GoldBarCelebration and the count-up pulse in GoldBarBadge stay in sync. */
export const COIN_BURST_MS = 260;
export const COIN_FLIGHT_MS = 420;
export const COIN_ARRIVAL_MS = COIN_BURST_MS + COIN_FLIGHT_MS;
export const BADGE_COUNT_UP_MS = 480;
export const CELEBRATION_VISIBLE_MS = 2800;
